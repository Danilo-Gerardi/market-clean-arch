import { FormatToRealPipe } from './format-to-real.pipe';

describe('FormatToRealPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatToRealPipe();
    expect(pipe).toBeTruthy();
  });
});
